# Pre-release recovery audit, 2026-09-10

Review target: v0.4.2 application sources, bd243b1 and subsequent test-only fixes.
No merge/release performed. Existing dirty design documents are excluded.

Inventory: 51 unique patch-distinct non-ancestor branch commits; 204 reflog-only
commit references, with 155 exact patch equivalents and 43 remaining patch
variants (six entries collapsed or without a patch); no stashes. Three locked worktrees retain
nine *_feat source snapshots, all identical to blobs in candidate ancestry.
The existing recovery audit identifies full_review.diff as a historical aggregate;
today the nine source snapshot identities were rechecked. No worktree contents
were cherry-picked or deleted.

Reconciliation:
- Optional, self-paced cheek input: current CheekGestureCalibratorTest covers
  one unlabeled movement, neutral-only rejection and negative-going movement;
  legacy six-registration loops are superseded. Full optical gate still required.
- Blink rearm/asymmetry: BlinkGestureClassifierTest and
  BlinkDetectionParametersTest preserve the recovered behavior.
- Bounds/mirror/camera selection: CheekFaceBoundsQualityTest,
  FrontCameraSemanticMirrorTest, CameraSwitchCameraSelectionTest,
  CameraPreviewGeometryTest and UvcCameraSupportTest retain the boundaries.
- Orphan portrait-lock change 19b59c86: current CameraSetupManifestPolicyTest
  explicitly verifies portrait is requested for compact devices. Tablet window
  behavior remains subject to Android large-screen policies and device checks.
- Foreground lifecycle: core session tests and CameraSwitchPowerPolicyTest cover
  pause/resume. Device acceptance independently checks camera release on HOME
  and display-off, and process recreation.
- Natural blink negative / stale presenter / public source changes: present in
  manifest controls and current optical tests; 129 host harness tests pass.
- Cumulative timing / legacy APK capture variants: current e2e-web.mjs retains
  scenarioCumulativeScanTiming and scripts/measure-android-scan-timing.ps1 exists.
  Diagnostics and version-code bisect variants are investigation artifacts, not
  unmerged production fixes; do not restore reduced dictionaries/version bumps.
- Main-branch Apache relicense ff84de8d: LICENSE blob matches exactly.
- Direct-MPImage and rotation-copy performance branches: experiments, not fixes;
  retain outside candidate. No new optimization claim or merge without fresh
  comparable device quality/performance evidence.
- Old publishing, review prose, and abandoned refactor/revert patch variants:
  historical evidence. Current source and tests own release behavior.

The independent unresolved issues discovered by this review are recorded in
v0.4.2-review-20260910.md, rather than inferred from branch subjects alone.
Inventory and blob/patch matches are preserved under .tmp/pre-release-20260910.
