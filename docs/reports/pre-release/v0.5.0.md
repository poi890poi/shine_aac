# v0.5.0 release candidate review — September 12, 2026

Status: **NOT RELEASED; gates remain open.** Approved changes are pushed on
`codex/release-v0.5.0`. Publication awaits the checks and layout review below.
The annotated `v0.5.0` tag has not been created or pushed.

## Candidate scope and recovery audit

Prepared in an isolated `codex/release-v0.5.0` worktree from published base
08020fb. Commit 84b860e contains the approved game/training/UI changes; b0a3d88
corrects provenance reporting for reused optical geometry. The original working
directory and its unfinished diagnostic changes are preserved. Local diagnostic
report commit c468831 is not in candidate ancestry or the outgoing source payload.

Commit 053f6d7 corrects the tagged CI artifact path: the obsolete universal-APK
path matched zero built files after ABI splitting. The corrected glob was checked
against all four real ABI outputs and now treats no matches as an error. CI keeps
read-only repository permissions and marks those disposable-key APKs as not for
distribution. Hosted tag CI has not run; public APKs must still come from the
pinned host build.

The branch, reflog, stash and locked-worktree inventory was refreshed. There are
269 non-ancestor commits across the retained historical references and reflogs,
no stashes, and nine locked-worktree source snapshots whose current blobs all
appear in candidate ancestry. The only non-ancestor commit created after the
previous recovery audit is c468831, a deliberately excluded diagnostic report.
Historical patch variants remain reconciled by the
[previous recovery matrix](v0.4.2-recovery-audit-20260910.md): blink rearm and
asymmetry, self-paced cheek calibration, camera bounds/mirror/selection, compact
portrait setup, foreground lifecycle and scan deadlines retain their equivalent
tests. Experimental direct-image/rotation branches remain excluded. No saved
worktree or user files were deleted or silently merged.

## Verified so far

- Clean dependency install; architecture and documentation checks pass.
- 322 core tests, 53 web unit tests and 96 garden unit tests pass.
- 124 native tests pass: 60 optical-core, 51 Android input and 13 app tests.
- Canonical four-ABI debug build and signed Play AAB build pass. Direct-install
  certificates match `config/direct-debug-cert.sha256`; the AAB reports
  `jar verified`. Its usual self-signed-chain, timestamp and POSIX metadata
  warnings are not evidence of Play acceptance.
- Source and packaged garden integration pass. The earlier arcade implementation
  review established pixel equality to approved A, accessible target bounds,
  six viewport/density combinations, renderer parity and lifecycle restoration.
- The full source browser suite reaches and reproduces the outstanding
  large-English-suggestion failure. It must pass in source and packaged modes
  after the correction; the assertion has not been weakened.

## Release gate status

### Large-text readability

At a 320 × 694 CSS-pixel viewport and requested 40 px text, ACCESSIBILITY retains
two columns and fits down to 15.7 px. The content-only span cache deliberately
avoids moving scan targets on resize, but also prevents a roomier layout when
text/window geometry changes. Any correction must preserve active scan targets
and timing, as well as readable labels.

A full-width, large-text visual proposal was shown to the user before app
changes, as requested. Its illustrative two-line word break is awaiting review;
it is not an implemented hyphenation algorithm or a passing test result. The
existing one-line board policy must not be silently changed. The game HUD's
one-row requirement and AAC replay's two-line status are separate invariants.

### Optical end-to-end coverage — complete on Preview

An attempt to update the tablet's main package stopped at signature mismatch.
Inspection established that its installed 0.4.1 is Play-signed, whereas the phone's
installed 0.4.2 has the pinned direct-install certificate. No app was uninstalled.
The tablet Preview package provides a separate upgrade-compatible test surface;
canonical direct-install upgrade verification belongs on the phone.

The first complete blink-use attempt using an older fixture failed at its initial
activation. Camera analysis was running without face scores. This is not proof
of a detector regression: fresh atlas discovery found a different framing, and
fresh native long-blink calibration subsequently passed with good saved quality.
The corrected-fixture blink communication scenario passed: 20 selections and
four speech turns (`幫忙喝水`, `不舒服休息`, `痛幫忙`, `hi`), including a deliberate
wrong choice, Undo, clear and an English/Zhuyin round trip. It used 49 observed
camera activations over 1,315.944 seconds. No repeated detector/stress sequences
were run. Both displays were verified OFF after restoration. Evidence is in
`test-results/optical-20260912-093806/`.

Fresh native cheek calibration passed using the manifest's public smiling
source. Measured face height was 65.6% of the preview, near the approximately
70% target; the model was accepted after one unlabeled twitch sequence in
8.7 seconds. Evidence is in `test-results/optical-20260912-100120/`. Restoration
and both display-OFF checks passed.

The separate cheek communication scenario also passed: 17 selections and four
speech turns (`廁所`, `累`, `家人`, `ok`), including correction, clear and the
English/Zhuyin round trip. It used 40 observed camera activations over 987.821
seconds. Evidence is in `test-results/optical-20260912-100310/`. No repeated
detector/stress sequences ran. Settings restoration and both display-OFF checks
passed; both device mutexes and the acknowledged window were explicitly released.

Both complete normal-use optical gates are closed for this Preview candidate.
These public-media rig results do not establish cross-user accuracy or sustained
thermal performance. Final main-package acceptance remains separate.

The report previously labelled reused geometry as an atlas PASS. A new real-report
test reproduced that error before the fix. The report now says
`REUSED (not verified this run)`; all 107 optical harness tests pass. Only a fresh
successful atlas is reported as a current geometry PASS.

### Final package and publication

Final source/packaged browser gates and physical main-package acceptance/upgrade
must pass before publication. After final
source changes, rebuild and verify the exact versioned APK/ZIP/AAB, publish only
those tested artifacts, and verify the annotated tag's remote peeled target.
The Play upload and tester-track acceptance have not occurred.

## Evidence and cleanup boundaries

Only the four checksum-verified public stimuli in the optical manifest were used.
Camera frames, application preferences and raw device logs remain in ignored
local evidence; they are not release assets. Device operations use acknowledged
windows and both shared mutexes through settings restoration and display-OFF
verification. Failed runs stop before stress testing. Raw fixture reuse does not
establish current physical geometry, and calibration alone does not establish a
complete communication session.

Internal candidate evidence is under `.tmp/release-v050/` and `test-results/`
inside the isolated worktree. Provisional packages are under its
`.artifacts/releases/v0.5.0/`; none is a public download or final released artifact.
