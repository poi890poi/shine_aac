# v0.5.0 delivery correction

Type: release packaging defect; application behavior and artwork are unchanged.

Cause: the release flow copied the detailed version changelog to both artifact
paths while `docs/PLAY_RELEASE_NOTES.md` still described v0.4.1/code61. The
documented Play format was not an executable packaging gate. This allowed the
same user-facing mistake to recur despite the previous written instructions.

Fix: a current-version JSON source generates the pure language-tagged Play
payload. Both packagers validate version, existing listing locales (`zh-TW`,
`en-IN`), 500-character limits and exact generated-document equality before
packaging. Both export `PLAY_RELEASE_NOTES.txt`; the APK ZIP includes it.
The former document fails the new check; six regression cases cover exact
output, stale name/code, Unicode length, locales, invalid text and current data.
The corrected translations contain 132 and 381 Unicode characters respectively.
Both actual packaging commands passed with `-SkipBuild`, exporting identical
Play payloads while preserving the tested APK and signed AAB hashes. This was
packaging verification, not a new application build or physical-device test.

## Download size findings

The tested ARM64 APK is 53,498,593 bytes. Its stored ZIP entries contain:

| Content | Decimal MB |
| --- | ---: |
| Web/game assets and code | 26.08 |
| Native libraries | 12.15 |
| Android bytecode | 8.14 |
| Face model | 3.35 |
| Other resources | 2.02 |

Within game assets, candidate sheets account for 19.91 MB and a cloud review
PNG for 1.25 MB. The builder copies whole scenery/native directories and three
candidate folders. Some old sprite paths remain supported and the flower
source is still loaded, so deleting every candidate image is not yet justified.
This is an identified packaging optimization opportunity, not an app-size fix
already verified. Runtime dependency pruning needs its own asset-load checks.

The previous 172.17 MB download ZIP contains four complete ABI-specific APKs.
It is a distribution bundle, not one device's installed size. A separate ARM64
ZIP now supplies the same tested APK and the corrected Play notes for the
connected phone/tablet. No new physical test is claimed for a notes-only change.
The smaller ZIP is 42,699,910 bytes (SHA-256
`79d44d53046599a6320f400bbeb4f6c598c4ddef109fd0d7e3745c043d39bd99`).
The corrected Play payload and smaller ZIP were downloaded through the existing
user-requested Cloudflare tunnel and verified byte-for-byte against local files.

## Encoding delivery regression

The user subsequently observed corrupted Chinese in the plain-text download.
The UTF-8 bytes were valid, but the temporary server sent `text/plain` without
a charset. A real browser reproduced `document.characterSet=windows-1252` and
text unequal to the canonical notes. Thus the earlier byte/hash verification
was insufficient to establish readable delivery.

The reusable `scripts/serve-release-downloads.py` declares UTF-8 for text
responses. The same public endpoint now returns `text/plain; charset=utf-8`;
a fresh browser reports UTF-8 and exact canonical-text equality. A real HTTP
regression covers header-driven Chinese decoding and unchanged binary download
bytes. No note wording, APK, or AAB changed. The download server was replaced on
its existing loopback port after verifying process ownership; its tunnel stayed
running, preserving the shared URLs.

## Wrong binary in the release handoff

The delivered page led with debug APKs even though the user requested a release
and was preparing Google Play notes. The correct Play binary had already been
built but was omitted from the public handoff. This was a delivery-process
failure: successful APK tests do not satisfy a Play AAB handoff.

`prepare-play-downloads.py` now prepares the Play page and ZIP from the current
versioned AAB, its checksum, and the validated notes. It rejects a renamed APK,
incorrect checksum and stale notes. The regression checks actual ZIP contents
and bytes. The page identifies the signed AAB as the Play upload, with the
notes and checksum beside it, and explicitly leaves Play acceptance pending.

Google bundletool 1.18.3 validates the AAB; its decoded manifest confirms
`org.shineaac.app`, version 0.5.0/code63 and `debuggable=false`. `jarsigner`
reports `jar verified`; the upload certificate SHA-256 is
`AF52D7C16A8A155762C7AD81BCDC67B2871A86CC26059ECE1804B0FCCED6DCC7`.
The application source matches tested implementation a9d3e94; later commits
only correct tooling/documentation. Existing branch/reflog audit has no new
non-ancestor fix branch since the earlier recovery audit, and no stashes exist.

Public Cloudflare downloads of all three handoff files match local bytes:

- AAB (58,490,922 bytes):
  `1276e123463722786cce279dd6e6682c84f1e3009db717f3a1cec406d60f1e2f`.
- Play ZIP (58,141,290 bytes):
  `bd9564f3d1b8477fc1799a2ea33027152cc3953f7a4a8deaa4865d9973d48989`.
- Notes (819 UTF-8 bytes):
  `11efc7973f8dfb328503fbc6f29f72a94901e8dd498d7c3a4b098da76c4a1549`.

All eight release-note/download tests pass, including the real HTTP encoding
case. No app rebuild, APK substitution, Play upload or Play acceptance is
claimed for this handoff correction. The Play ZIP contains an AAB, not an APK.
