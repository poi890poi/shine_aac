# Cheek stimulus admission failure

The tablet CPU calibration attempt `optical-20260911-191449` stopped after its first public cheek trial. The preview was 906 pixels tall and the observed face overlay 448 pixels tall (49.45%), below the unchanged 55–82% gate. No cheek model or GPU quality result is admitted from this attempt. APK, preferences, permission grant state and display settings were restored; both displays verified OFF.

The source manifest's `face_height_at_zoom_1x=0.26` is an empirical ratio, not an intrinsic property of the video. `case_display_scale` multiplies that ratio by the current camera zoom while ignoring the camera/presenter projection and preview geometry. At 3.2x it selected scale 0.84135; the photographed result contradicts the predicted 70%. The same atlas-calibrated setup passed blink framing. This identifies the rig's portable-scale assumption as the failing boundary, rather than evidence that gesture thresholds are wrong.

Falsifiable correction: before native cheek calibration, measure the declared source's relaxed face in the current preview, adjust only its presenter scale by target/measured coverage, then measure again. Freeze the resulting source scale with the camera/presenter/zoom session fixture for CPU/GPU runtime comparisons. Keep the public source bytes, detector thresholds, zoom, timing and framing gate fixed. A repeat should reach approximately 70% without clipped face bounds; otherwise stop again and retain the failure. Do not tune the source scale from activation success or gesture scores.

The focused cheek setup also constructs a reduced calibration summary that omits the already verified stimulus orientation, although its save path requires that field. Cover and correct that independent metadata handoff before declaring focused session reuse valid.

The first normalization repeat (`optical-20260911-192519`) retained a second measurement failure. Scaling from 0.84135 to 1.14013 visibly enlarged the face, but the old oracle clipped every pixel in the bottom 12% of the preview to exclude the progress meter. Its reported bottom was exactly that artificial cutoff, 983 px, while the saved screenshot's face outline extended to 1019 px. A regression reproduces an outline ending at 940 px being falsely truncated to 880 px. The corrected oracle follows long vertical outline runs and separates the progress meter's shorter run instead of cropping a band. All 105 optical harness tests pass. Reprocessing the immutable screenshot now measures 69.21%, rather than 65.23%. This explains the false rejection; it does not turn the aborted run into a completed calibration. A fresh physical repeat is still required.

The next attempt (`optical-20260911-193051`) exposed short occlusions in the new outline-run detector. Its first measurement wrongly truncated a visible outline at 734 px and then requested excessive scale; the next image correctly failed to yield a complete face outline. Pixel inspection found 3–6 px gaps where the app draws blue landmarks over the green box. `CameraFaceOverlayView` draws 2.2-px-radius dots after the box. A new regression fails on that interruption; allowing up to eight pixels of local occlusion restores the full 919-px bottom while still separating the progress bar. The same immutable images now measure 51.55%, 69.21%, and 51.77% as visible; the excessively scaled image remains inadmissible. All 106 tests pass. Failed attempts remain failed, and the upcoming physical run must establish calibration afresh.

Fresh confirmation `optical-20260911-193647` passed: relaxed face 69.3% at presenter scale 1.14013 and unchanged 3.2x camera zoom; post-trial face 67.5%; native calibration saved a good-quality model after one 8.9-second sample sequence. The focused session cached its verified orientation and source scale. Its guarded cleanup verified exact preferences, original APK, original permission grant state, display settings and both displays OFF. This is physical framing/calibration evidence; CPU/GPU cheek runtime comparisons remain a separate gate.
