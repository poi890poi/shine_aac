import {readFileSync,writeFileSync} from 'node:fs';
import {createRequire} from 'node:module';
const require=createRequire(process.env.SHINE_PLAYWRIGHT_ROOT||import.meta.url);
const {chromium}=require('playwright');
const root=process.argv[2]||'.tmp/visual-review-20260909';
const mountains=JSON.parse(readFileSync(new URL('../apps/bird-minigame/assets/scenery/mountain-variants-attribution.json',import.meta.url),'utf8'));
writeFileSync(`${root}/artwork-attribution.txt`,[
  'SHINE AAC visual review — mountain artwork attribution',
  'The game randomly selects one of these licensed mountain adaptations.',
  'Adaptations: crop, background removal, pixel reduction, reduced blue-green palette and atmospheric tint. Adapted mountain images retain their source licenses. UI screenshots are synthetic review fixtures.',
  ...mountains.map(m=>[m.title||m.label,m.author,m.sourcePage,m.license,m.licenseUrl,m.adaptation||'Pixel reduction and palette conversion'].join('\n')),
].join('\n\n'));
const src=name=>'data:image/png;base64,'+readFileSync(`${root}/${name}.png`).toString('base64');
const style=`<style>*{box-sizing:border-box}body{margin:0;padding:36px;background:#edf1f5;color:#172331;font:20px/1.5 Arial,sans-serif}h1{font-size:32px;margin:0 0 8px}h2{font-size:23px;margin:0 0 14px}p{margin:8px 0 22px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}.card{background:white;padding:22px;border:1px solid #c9d1d9;border-radius:12px}.crop{width:412px;height:800px;overflow:hidden;margin:auto}.crop img{width:1280px;max-width:none}.tag{font-size:16px;font-weight:bold;color:#395d80}.full{width:100%;display:block}.note{background:#fff2d9;padding:16px;border-left:5px solid #be7a18;margin-top:24px}small{color:#526272}</style>`;
const comparison=`${style}<h1>Tablet message panel — review before changes</h1><p>Synthetic browser capture • 1280 × 800 • board outside this crop is identical.</p><div class="grid"><div class="card"><div class="tag">CURRENT APP</div><h2>Clipped history, unused space</h2><div class="crop"><img src="${src('tablet-landscape')}"></div></div><div class="card"><div class="tag">PROPOSAL — NOT IMPLEMENTED</div><h2>Readable history, anchored helpers</h2><div class="crop"><img src="${src('tablet-panel-proposal')}"></div></div></div><div class="note">Design judgment: give the message area a clear hierarchy. This preview increases the draft area, gives each previous message enough height, and moves helpers to the bottom. No artwork, scanning, or board changes.</div>`;
const defects=`${style}<h1>Phone review — current app, no changes</h1><p>Browser viewport evidence. These are design-review images, not physical-device screenshots.</p><div class="card"><div class="tag">800 × 360 LANDSCAPE • FUNCTIONAL DEFECT</div><h2>The board fits its container, but its labels are unreadable.</h2><img class="full" src="${src('phone-landscape')}"><p>All rows are compressed into the remaining height. A bounds-only test can pass this. A proposed responsive interaction needs review before implementation.</p></div><div class="grid" style="margin-top:24px"><div class="card"><div class="tag">360 × 800 PORTRAIT • DESIGN QUESTION</div><img src="${src('phone-garden')}" style="display:block;width:360px;margin:16px auto"></div><div class="card"><h2>Does this composition feel natural?</h2><p>Flowers have very long stems relative to their heads. Mountains sit near the bottom, leaving a large sky region.</p><p>Preserve the approved curved stems and sprite shapes. Review the portrait scene proportions and camera framing before changing them.</p><p>The bird is partly outside the left edge during fly-in; a single frame cannot establish a motion defect. Judge that with the sequence.</p><p>Also compare the smooth helper-button outlines with the pixel-art scene. This is a styling judgment, not a geometry failure.</p><div class="note">No flower, stem, cloud, bird, mountain or app styling edits have been applied.</div></div></div>`;
const browser=await chromium.launch({channel:'msedge',headless:true});
try{for(const [name,html] of [['tablet-panel-review',comparison],['phone-design-review',defects]]){
  writeFileSync(`${root}/${name}.html`,html);
  const page=await browser.newPage({viewport:{width:1040,height:900},deviceScaleFactor:1});await page.setContent(html);await page.screenshot({path:`${root}/${name}.png`,fullPage:true});await page.close();
}}finally{await browser.close();}
